# Security config generated by DevSecOps Kit

version: "0.2.0"

language: "golang"
framework: ""

severity_threshold: "high"

tools:
  semgrep: true
  trivy: true
  gitleaks: true

# Reserved for future versions — currently unused in execution
exclude_paths: []

# Reserved for future: per-tool fail-on behavior
fail_on: {}

# Notification settings (PR comment enabled by default)
notifications:
  pr_comment: true
  slack: false
  email: false
